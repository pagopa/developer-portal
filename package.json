{
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "prepare": "husky install",
    "clean": "turbo run clean",
    "compile": "turbo run compile",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "@types/html-minifier": "^4.0.4",
    "@types/mjml": "^4.7.4",
    "husky": "^8.0.3",
    "turbo": "^1.10.12"
  },
  "dependencies": {
    "html-minifier": "^4.0.0",
    "mjml": "^4.14.1"
  }
}
