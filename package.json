{
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "prepare": "husky install",
    "clean": "turbo run clean",
    "compile": "turbo run compile",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "turbo": "^1.9.1"
  }
}
