---
"nextjs-website": minor
---

Add validateName to validate fields like name, surname and role during registrations and profile update, update validateField to check for maximum lenght, update sign up form to implement the new validation logic, add validation login for name, surname and role in ProfileDataCard, update RequiredTextField to showhelper text when the field is in error state and fix typo in input prop from maxlength to maxLength
