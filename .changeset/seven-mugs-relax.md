---
"extractor": major
---

Extractor app that, given a folder input path with the parser output, processes all .json files in that folder writing in the output folder a cleaned and markdown formatted version through an LLM call with structured output
